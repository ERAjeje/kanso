---
status: testing
phase: 07-03-integracao
source:
  - 07-03-01-SUMMARY.md
  - 07-03-02-SUMMARY.md
  - 07-03-03-SUMMARY.md
  - 07-03-04-SUMMARY.md
started: 2026-05-23T18:30:00Z
updated: 2026-05-23T18:30:00Z
---

## Current Test

number: 1
name: Emotion chips appear in RegistroCard when analise data exists
expected: |
  In the History page, each registration card that has NLP analysis data shows colored emotion chips below the sentimento name. The principal emotion appears as a larger chip, and up to 3 secondary emotions appear next to it.
awaiting: user response

## Tests

### 1. Emotion chips in RegistroCard
expected: Cards with analysis data show colored emotion chips below sentimentoNome (principal + up to 3 secondary)
result: [pending]

### 2. Graceful degradation — no chips when no analise
expected: Cards without analysis data show no emotion chips at all (not even empty containers)
result: [pending]

### 3. Emotion chip colors match palette
expected: Each of the 13 emotions has a unique Tailwind color (e.g., alegria → emerald, tristeza → blue, raiva → red, ansiedade → yellow)
result: [pending]

### 4. Chips visible in collapsed card state
expected: Emotion chips appear in the card header and are visible even when the card is collapsed (not expanded)
result: [pending]

### 5. PDF report — "Resumo das Emoções" section
expected: PDF report shows an "Resumo das Emoções" section with aggregate emotion frequency (each emotion name + count), sorted by count descending
result: [pending]

### 6. PDF report — per-registro emotion chips
expected: Each registro block in the PDF shows emotion chips below the sentimento line when analysis exists
result: [pending]

### 7. PDF report — emotion sections hidden when empty
expected: When no emotions exist in the report period, both the summary section and per-registro chips are hidden (no empty containers)
result: [pending]

### 8. PDF report — registros data actually fetched
expected: PDF report shows real registro data (fields: data, sentimento, sensações, contexto, pensamentos) instead of the pre-existing "Nenhum registro encontrado" state
result: [pending]

### 9. Watcher — NLP analysis triggered for new registros
expected: When a new registro is created, the watcher goroutine picks it up via _changes, calls gRPC NLP service, and stores analise:{registroId} in CouchDB. The frontend then displays emotion chips after PouchDB sync.
result: [pending]

### 10. Watcher — resilience on NLP failures
expected: If NLP service is unavailable, the watcher retries 3 times with exponential backoff (1s, 4s, 16s), then skips silently — no user-facing errors, checkpoint advances
result: [pending]

## Summary

total: 10
passed: 0
issues: 0
pending: 10
skipped: 0
blocked: 0

## Gaps

<!-- YAML format for plan-phase --gaps consumption -->
